# R17.8O.2 — Real Testnet Partial Payment Refund Validation

**Mode:** Live test validation + documentation (no code changes)  
**Date:** 2026-08-14  
**Depends on:** R17.8O (audit), R17.8O.1 (`0d85a93`)  
**Baseline commit:** `0d85a93`

---

## 1. Executive conclusion

| Layer | Verdict | Notes |
|-------|---------|-------|
| **Deployer wallet readiness** | **PASS** | Testnet balance **0.354 TON** ≥ 0.2 TON reserve; seqno **103** |
| **Automated unwind regression** | **PASS** | `partialPaymentEscrowUnwind.test.js`, `gameEscrowCancelE2E.test.js` |
| **Live 3-player partial-pay → cancel → refund** | **NOT EXECUTED** | Requires manual operator run on production/staging with 3 real devices |
| **Session archive (post-O.1 refund run)** | **NOT AVAILABLE** | No qualifying archive retrieved in this pass |
| **Overall R17.8O.2** | **CONDITIONAL PASS (environment + automated)** / **LIVE BLOCKED (pending manual run)** |

Environment preparation **succeeds** — the deployer wallet blocker from R17.8N (~0.007 TON) is **resolved**. A full real-device refund lifecycle validation **cannot be closed** until an operator completes Steps 1–4 below on a server running **`0d85a93` or later**.

---

## 2. Test environment

| Item | Value |
|------|--------|
| **Validation date** | 2026-08-14 |
| **Repository** | `wheelwin` @ `0d85a93` |
| **Network** | TON **testnet** |
| **Frontend (production)** | `https://wheelwin-nine.vercel.app` |
| **Backend (production)** | `https://wheelwin-production.up.railway.app` |
| **Deploy mode** | Live GameEscrow (`GAME_ESCROW_MODE=game`) |
| **Stake (configured)** | **1 Gram** per seat (3 seats → 3 Gram expected total) |
| **Automated runner** | Node.js v24.14.1, Windows dev host |
| **Real-device matrix (planned)** | Player A — Telegram WebApp; Player B — Telegram WebApp; Player C — mobile browser or Telegram |

**Pre-requisite before live run:** Confirm Railway production deploy includes commit **`0d85a93`** (R17.8O.1 escrow unwind). O.1 code must be live for automatic `EMERGENCY_CANCEL` on partial payment failure.

---

## 3. Deployer wallet readiness (`/debug` → TON Deployer Wallet)

Queried via `node server/scripts/check-testnet-wallet-readiness.js` and TonCenter public API (2026-08-14):

| Field | Expected | Observed |
|-------|----------|----------|
| **Network** | `testnet` | `testnet` ✓ |
| **Wallet address** | Deployer V4R2 | `EQB83s9XMOMseDFxyXxj4hrC0sS4FB4xhdNiUPkl_3zx3PDQ` ✓ |
| **Wallet type** | `WalletContractV4R2` | `WalletContractV4R2` (`walletId: 698983191`) ✓ |
| **Balance** | ≥ **0.2 TON** (READY) | **0.354381195 TON** ✓ |
| **Seqno** | Available | **103** (advanced from **100** at R17.8L) ✓ |
| **Readiness** | **READY** | **READY** (`DEPLOYER_WALLET_READY`) ✓ |

### Comparison to R17.8N (prior blocker)

| Metric | R17.8N (2026-08-14 AM) | R17.8O.2 (2026-08-14 PM) |
|--------|------------------------|---------------------------|
| Balance | ~0.007 TON | **0.354 TON** |
| Seqno | 100 | **103** |
| Readiness | ERROR (`INSUFFICIENT_DEPLOYER_BALANCE`) | **READY** |
| Live deploy | Blocked | **Unblocked** |

### Production `/console/ton/deployer-wallet`

| Access | Result |
|--------|--------|
| Unauthenticated | **401 Unauthorized** (expected — Administrator JWT required) |
| `/debug` UI | Login required; panel not queried with credentials in this pass |

No mnemonic, private keys, or API secrets recorded.

---

## 4. Real test scenario — execution status

### Step 1 — Create game

| Action | Status |
|--------|--------|
| CREATE_ROOM → 3 players join → VERIFY → PAYMENT_READY | **NOT EXECUTED** |

### Step 2 — Deploy escrow

| Expected event | Status |
|----------------|--------|
| `CONTRACT_DEPLOYING` / `BEGIN_DEPLOY` | **NOT EXECUTED** |
| `GAME_CONTRACT_DEPLOY_PREFLIGHT_PASSED` | **NOT EXECUTED** |
| `GAME_CONTRACT_DEPLOYED` | **NOT EXECUTED** |
| `GAME_CONTRACT_READY_FOR_PAYMENTS` | **NOT EXECUTED** |

| Record field | Value |
|--------------|-------|
| Contract address | — |
| Deploy transaction | — |
| Timestamp | — |

### Step 3 — Partial payment

| Player | Expected | Status |
|--------|----------|--------|
| Player A | `PAYMENT_CONFIRMED` | **NOT EXECUTED** |
| Player B | `PAYMENT_CONFIRMED` | **NOT EXECUTED** |
| Player C | `AWAITING_PLAYER_CONFIRMATION` (unpaid) | **NOT EXECUTED** |

Expected session: `PARTIALLY_PAID`, confirmed **2**, unpaid **1**.

### Step 4 — Trigger failure (timeout or creator leave)

| Check | Expected | Status |
|-------|----------|--------|
| Immediate `ROOM_DESTROYED` | **Must NOT occur** | **NOT VERIFIED** |
| `ESCROW_CANCEL_REQUESTED` | Present | **NOT OBSERVED** |
| On-chain `EMERGENCY_CANCEL` | Broadcast + confirmed | **NOT OBSERVED** |
| `ESCROW_CANCEL_CONFIRMED` | Present | **NOT OBSERVED** |
| `ESCROW_REFUND_PENDING` | Present | **NOT OBSERVED** |
| `ESCROW_REFUND_CONFIRMED` (×2) | Present | **NOT OBSERVED** |
| `PAYMENT_SESSION_FAILED` → `ROOM_DESTROYED` | After refunds | **NOT OBSERVED** |

---

## 5. Blockchain validation

No live partial-payment refund session was completed in this pass. Fields below are **placeholders** for the operator checklist.

### Contract

| Field | Value |
|-------|-------|
| Contract address | *(pending live run)* |

### Cancel transaction

| Field | Value |
|-------|-------|
| Transaction hash | *(pending)* |
| Timestamp | *(pending)* |
| Status | *(pending)* |

### Refund transactions (per paid player)

| Player | Wallet | Refund tx hash | Amount | Status |
|--------|--------|----------------|--------|--------|
| A | *(pending)* | *(pending)* | *(pending)* | *(pending)* |
| B | *(pending)* | *(pending)* | *(pending)* | *(pending)* |

---

## 6. Session archive validation

| Check | Result |
|-------|--------|
| Archive downloaded | **No** — no post-O.1 partial-refund session available |
| `payment.status` shows refund lifecycle | **NOT VERIFIED** |
| `blockchainLifecycle` includes cancel + refunds | **NOT VERIFIED** |
| `PARTIALLY_PAID` + `ROOM_DESTROYED` without escrow events | **Must NOT occur** after O.1 — **NOT VERIFIED live** |

**Historical incident (`ROOM_eVWg`, pre-O.1):** Archive showed `PARTIALLY_PAID` at `ROOM_DESTROYED` with no automatic cancel — the gap O.1 addresses. Re-test required on O.1+ deploy.

---

## 7. Observability validation

### Automated (executed)

| Test | Events / behavior verified | Result |
|------|---------------------------|--------|
| `partialPaymentEscrowUnwind.test.js` A | Cancel called; refund watch; deferred `PAYMENT_SESSION_FAILED` | **PASS** |
| `partialPaymentEscrowUnwind.test.js` B | Full payment → cancel **not** called | **PASS** |
| `partialPaymentEscrowUnwind.test.js` C | `creator_left` → escrow cancel | **PASS** |
| `partialPaymentEscrowUnwind.test.js` D | `ESCROW_CANCEL_FAILED`; contract preserved | **PASS** |
| `gameEscrowCancelE2E.test.js` | Partial stake → cancel sync → refund observation | **PASS** |
| `paymentPhaseRecovery.r178b.test.js` | Recovery regression | **PASS** (prior run) |
| `recoveryIntegration.r178d.test.js` | Recovery regression | **PASS** (prior run) |

### Live (required, not executed)

| Event | Required in live run |
|-------|---------------------|
| `ESCROW_CANCEL_REQUESTED` | Yes |
| `ESCROW_CANCEL_CONFIRMED` | Yes |
| `ESCROW_REFUND_PENDING` | Yes |
| `ESCROW_REFUND_CONFIRMED` | Yes (per refunded seat) |

If `ESCROW_CANCEL_FAILED` occurs during live run: **do not** manually destroy the room; verify contract reference preserved in `GameContractManager`.

---

## 8. Operator checklist (complete live R17.8O.2)

1. Confirm Railway deploy ≥ `0d85a93`.
2. Open `/debug` → **TON Deployer Wallet** → confirm **READY** (balance ≥ 0.2 TON).
3. Run 3-player room through VERIFY → PAYMENT_READY.
4. Confirm deploy events and record **contract address** + **deploy tx**.
5. Pay Player A + B only; leave Player C unpaid → confirm `PARTIALLY_PAID`.
6. Trigger **payment timeout** or **creator leave**.
7. Verify event sequence (§7) in Developer Console / session archive — **no immediate `ROOM_DESTROYED`**.
8. Record cancel tx + per-player refund txs on testnet explorer.
9. Download session archive; confirm `blockchainLifecycle` + escrow events.
10. Update this document §4–§6 with live data and set overall verdict to **PASS** or **FAIL**.

---

## 9. Remaining limitations

- **Manual execution required** — This validation pass prepared environment and automated regression only; 3-device live run is operator-owned.
- **Production auth** — `/console/*` endpoints require Administrator login; live observability depends on authenticated `/debug` access or archive download.
- **Deploy sync** — Production must run O.1 code before live test; Vercel frontend alone does not deploy server changes.
- **Stub deploy mode** — Local `deployMode=stub` cannot exercise real `EMERGENCY_CANCEL`; live test must use live adapter on testnet.
- **Cancel failure path** — If on-chain cancel fails, room and contract are intentionally preserved; manual recovery may be needed (documented in R17.8O.1).

---

## 10. Final verdict

| Scope | Verdict |
|-------|---------|
| Environment preparation | **PASS** |
| Automated O.1 unwind regression | **PASS** |
| Real testnet 3-player refund lifecycle | **PENDING — NOT EXECUTED** |
| **R17.8O.2 overall** | **CONDITIONAL — awaiting live operator run** |
