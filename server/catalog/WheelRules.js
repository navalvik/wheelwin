export const WHEEL_RULES = Object.freeze({
    minSectors: 3,
    maxSectors: 6,
    defaultRotation: 0,
    defaultTriangleRatio: Object.freeze({
        height: 0.04,
        width: 0.03
    })
});
