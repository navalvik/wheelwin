/**
 * R17.9I.4 — Editable Audio Registry override fields.
 * id / file / category remain code-controlled.
 */

export const AUDIO_REGISTRY_EDITABLE_FIELDS = Object.freeze([
    "enabled",
    "loop"
]);
