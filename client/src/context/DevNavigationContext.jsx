import { createContext } from "react";

/**
 * Developer Stub — optional JUMP navigation from App (DEBUG_JUMP_ENABLED).
 * Provider value is null when the stub is disabled.
 */
export const DevNavigationContext = createContext(null);
