/**
 * R17.9I.3 — Editable Audio Registry override fields.
 * eventId / audioFile / category remain code-controlled.
 */

export const AUDIO_REGISTRY_EDITABLE_FIELDS = Object.freeze([
    "enabled",
    "volume",
    "loop"
]);
