# R17.8V.1B / V.1C — Testnet ValueTon Matrix

**Mode:** Measurement tooling (no production `valueTon` reduction)  
**Harness:** R17.8V.1C + V.2C player wallet init fix  
**Date updated:** 2026-08-15 (R17.8V.2E CANCEL value formula audit)

## Execution status

| Item | Status |
|------|--------|
| Measurement harness | **ADDED** (`server/tests/testnet/runValueTonSweep.js`) |
| Environment audit (V.2A) | **DONE** — readiness **READY** |
| Baseline live run (V.2B) | **EXECUTED** — A PASS; B/C FAIL (wallet init) |
| Player wallet init fix (V.2C) | **DONE** — B PASS; C PASS @ 0.05 |
| Reduced matrix (V.2D.1) | **EXECUTED** — full PASS through 0.03; 0.025/0.02 incomplete (RPC) |
| Boundary completion (V.2D.2) | **EXECUTED** — **0.025 fully validated**; **0.02/B VALUETON_FAILURE** |
| CANCEL formula audit (V.2E) | **DONE** — source formula explains 0.020/0.025 boundary |
| Production `valueTon` | **0.05** (unchanged) |
| Contract / ABI | **Unchanged** (audit only; no code edits) |

---

## R17.8V.2E — CANCEL Value Formula Audit

**Audit date:** 2026-08-15  
**Mode:** READ-ONLY (no chain txs; no production code changes)  
**Sources:** `contracts/game_escrow/GameEscrow.tact`, `server/payment/TonGameContractAdapter.js`, `server/payment/ton/testValueTonOverride.js`, harness `runValueTonSweep.js`

### 1. CANCEL source path

Handler: `receive(msg: EmergencyCancel)` in `GameEscrow.tact` (opcode `0x43414e43` / `"CANC"`).

Flow:

```text
oracle EmergencyCancel (+ attach valueTon)
        ↓
require sender == oracle
require not settled
require status ∈ {PAYMENTS_OPEN, READY}
        ↓
pendingRefund = Σ stake_i for bits in paidMask \ refundMask
        ↓
require myBalance() >= pendingRefund + CANCEL_GAS_RESERVE
        ↓
send stake_i to each unpaid-refund seat (SendPayGasSeparately, bounce=false)
        ↓
refundMask |= bits; refundedAmount += …; status = CANCELLED
```

**There is no `context().value` check on CANCEL.** Only `myBalance()`.

### 2. Constants (CANCEL / refund economics)

| Name | File | Location | Value | Unit | Purpose |
|------|------|----------|------:|------|---------|
| `CANCEL_GAS_RESERVE` | `GameEscrow.tact` | lines 21–22, used 266–268 | **0.08** | TON (`ton("0.08")`) | Cushion so up to 3 refund `send`s can pay forward fees |
| `SETTLE_GAS_RESERVE` | `GameEscrow.tact` | lines 18–19, used 220–221 | **0.05** | TON | SETTLE path only (not CANCEL) |
| Refund send `value` | `GameEscrow.tact` | 276–304 | exact `stake0/1/2` | coins | Exact stake refunded; mode `SendPayGasSeparately` |
| STAKE exactness | `GameEscrow.tact` | 201 | `context().value == required` | coins | Player deposit must match registered stake exactly |
| Production oracle attach | `testValueTonOverride.js` | `PRODUCTION_ORACLE_VALUE_TON` | **0.05** | TON | Default when override off |

No separate “refund gas reserve” constant beyond `CANCEL_GAS_RESERVE`. No storage / action-phase reserve constants in this contract beyond those two.

### 3. Exact CANCEL formula

Contract gate (source):

```text
myBalance() >= pendingRefund + CANCEL_GAS_RESERVE
pendingRefund = Σ stake_i  for i where (paidMask & bit_i) != 0 and (refundMask & bit_i) == 0
CANCEL_GAS_RESERVE = 0.08 TON
```

Under the **current measurement architecture** (uniform oracle `valueTon = V` for DEPLOY + INIT_GAME + OPEN_PAYMENTS + CANCEL; player stakes `S` each):

```text
balance_pre_cancel ≈ 3·V′ + N·S − G
myBalance_at_require ≈ balance_pre_cancel + C′
                     ≈ 3·V′ + N·S − G + C′
```

where:

- `V′` / `C′` = effective TON credited to the escrow from each oracle message (≤ configured `V` / cancel attach; oracle wallet uses default `SendMode.NONE`, so forward fees may reduce credited value);
- `N` = number of paid seats;
- `G` = compute/storage TON burned from the escrow before CANCEL.

Gate simplifies (stakes cancel) to:

```text
3·V′ + C′ − G  >=  0.08
```

With uniform `C′ ≈ V′`:

```text
4·V′ − G  >=  0.08
V′        >=  (0.08 + G) / 4
```

**Important:** minimum CANCEL attach alone is **not** `0.08 + pendingRefund` unless prior residual is zero. The contract cares about **total contract balance**, not CANCEL attach in isolation.

### 4. Attached value vs fees

| Path | Condition | Fee behavior |
|------|-----------|--------------|
| **STAKE** | `context().value == required` (exact) | Player harness uses `SendMode.PAY_GAS_SEPARATELY \| IGNORE_ERRORS` (mode 3) so exact stake arrives |
| **CANCEL** | `myBalance() >= …` (inequality) | No exact attach check; oracle adapter `createTransfer` does **not** set `sendMode` → library default **NONE (0)** → fees can come out of attach |
| **SETTLE** | `myBalance() >= winner+owner+0.05` | Same oracle send path |

### 5. Live comparison (explains 0.020 vs 0.025)

From V.2D.2 Scenario B @ 0.02 after failed CANCEL (stake still held):

```text
remainingBalanceTon ≈ 0.105737315
≈ 3·0.02 + 0.05 − G  ⇒  G ≈ 0.0043 TON
```

At CANCEL require (if attach had been accepted into balance):

```text
myBalance ≈ 0.1057 + 0.02 = 0.1257
needed    = 0.05 + 0.08 = 0.13
0.1257 < 0.13  → require fails  → refundMask stays 0
```

At 0.025 with similar `G`:

```text
myBalance ≈ 3·0.025 + 0.05 − G + 0.025 ≈ 0.15 − G ≥ 0.13  → PASS
```

**Source formula explains the observed boundary.** Missing cost component is prior escrow gas burn `G` plus any `SendMode.NONE` attach shortfall on oracle messages—not an undocumented second reserve.

### 6. STAKE vs CANCEL economics

| | STAKE | CANCEL |
|---|-------|--------|
| Required value | Exact registered `stake_i` (live tests: **0.05**) | None on `context().value` |
| Gate | Equality | `myBalance` vs refunds + **0.08** |
| Why 0.02 oracle ValueTon still allows STAKE | STAKE attach is the **player stake 0.05**, independent of oracle `V` | CANCEL success depends on **accumulated** oracle attaches + stakes vs **0.08** cushion |

### 7. Theoretical minimums (uniform V, S=0.05)

Using live-inferred `G ≈ 0.0043` and `V′ ≈ V`:

| Scenario | pendingRefund | Need myBalance ≥ | Approx. uniform V floor `(0.08+G)/4` | Known live |
|----------|-------------:|-----------------:|------------------------------------:|------------|
| A — 0 paid | 0 | 0.08 | ~**0.021** | 0.05 PASS; 0.02 A PASS (V.2D.1) |
| B — 1 paid | 0.05 | 0.13 | ~**0.021** | **0.025 PASS / 0.020 FAIL** |
| C — 3 paid | 0.15 | 0.23 | ~**0.021** | 0.025 PASS |

```text
theoretical minimum (uniform V, this G):  ~0.021 TON
confirmed live minimum (A+B+C):           0.025 TON
remaining uncertainty:                    exact G and V′−V shortfall vary by network load;
                                          0.021–0.024 not live-tested
```

### 8. Server adapter

`TonGameContractAdapter.cancel()` → `_sendOracleMessage({ operation: "CANCEL", valueTon: resolveOracleValueTon("CANCEL") })`.

Production: always **0.05** unless `TEST_VALUETON_OVERRIDE=true`.  
Harness: `applyUniformValueTon(V)` sets DEPLOY/INIT/OPEN/CANCEL/SETTLE env overrides to the **same** `V` — one generic ValueTon for all oracle messages (no CANCEL-specific reserve on the server).

### 9. Audit conclusion

```text
B. 0.025 TON is only the current confirmed live minimum;
   a lower boundary exists and should be tested.
```

Useful **unexecuted** candidates (uniform V, Scenario B focus):

```text
0.022, 0.023, 0.024
```

(`0.021` is too close to the inferred floor; treat as optional stretch only.)

Do **not** change production `0.05`.

---

## R17.8V.2D.2 Targeted Boundary Completion

**Execution date:** 2026-08-15  
**Network:** testnet  
**Order:** `0.025/B` → `0.02/B` → `0.02/C` (C gated on B success)  
**RPC health:** checked before each live run (`check-v2d2-rpc-health.js`)  
**SendMode (V.2C):** confirmed `PAY_GAS_SEPARATELY | IGNORE_ERRORS` (=3) on STAKE logs  
**Start balance:** 8.182730524 TON  
**End balance:** 7.755416871 TON  
**Spent this stage:** **0.427314 TON**  
**Artifacts:**  
- `server/tests/testnet/artifacts/valueton-sweep-v2d2-025-B.json`  
- `server/tests/testnet/artifacts/valueton-sweep-v2d2-02-B.json`  
**Production lifecycle / GameEscrow / Payment / Recovery / defaults:** **unchanged**

### Results (classified)

| Test | Result class | Notes |
|------|--------------|-------|
| **0.025 / B** | **PASS** | DEPLOY→INIT→OPEN→STAKE→CANCEL→REFUND; paidMask=1; refundMask=1 |
| **0.02 / B** | **VALUETON_FAILURE** | STAKE ok (paidMask=1); CANCEL broadcast ok; **on-chain refundMask=0**, refundedTotal=0, cancelReason=0 — refund did not execute |
| **0.02 / C** | **NOT RUN** | Gated: 0.02/B did not complete refund lifecycle |

### Root cause for 0.02 / B (ValueTon)

GameEscrow CANCEL requires:

```text
myBalance() >= pendingRefund + CANCEL_GAS_RESERVE
CANCEL_GAS_RESERVE = 0.08 TON
```

Scenario B with stake 0.05 needs **≥ 0.13 TON** on the contract at cancel (including incoming cancel attach).  
At oracle ValueTon **0.02**, accumulated DEPLOY+INIT+OPEN+CANCEL attaches + stake are insufficient after fees → EmergencyCancel `require` fails → no refund.  
At **0.025**, the same path completed with refundMask=1.

Not infrastructure (RPC healthy). Not wallet/harness (STAKE SendMode=3, wallet active, paidMask updated).

### 0.025 / B — measurements

| Field | Value |
|-------|-------|
| Classification | **PASS** |
| Contract | `EQCaE__L-1gL_89ApTtTP3bCg7BCjJTyhSqTHd0aYUjTSapC` |
| DEPLOY | `lXbtHxezMDnvsVcWjRAja5JgjQoQlXyE1giD13xfCek=` @ 0.025 |
| INIT_GAME | `H1/W5OLrzzBGRnRJaVGGYyit+k74JUWlpEGEtbfOPis=` @ 0.025 |
| OPEN_PAYMENTS | `+dESOunSMUS7njgtjCY+20YDcnaH7tCil7PnVhS+64o=` @ 0.025 |
| STAKE | value **0.05** (player); balance 0.129048462 → 0.078657526; sendMode=3 |
| paidMask | **1** |
| CANCEL | `bGvpwoEvzfOhnr2ZqcAfzTt8zzeMIhhfikTQaR8e4aE=` @ 0.025 |
| refundMask | **1** |
| remainingBalanceTon | 0.094753245 |

### 0.02 / B — measurements

| Field | Value |
|-------|-------|
| Classification | **VALUETON_FAILURE** |
| Contract | `EQDf8naaPphQ1DQPWUg-C2NMjGJ4-YFW46dySClVcwlSAK-p` |
| DEPLOY | `2wQmhtm4mk7FYHt/zkUsmuFky2uy6iuhBIos0Cq+L7E=` @ 0.02 |
| INIT_GAME | `vSSlxwTsf27iagDhDym8u3oreE54KdZAmwBzsvaGTrg=` @ 0.02 |
| OPEN_PAYMENTS | `O7jeGwwowkQuqxTpAydrHMt2gE1toOHkCLRO21GNdzw=` @ 0.02 |
| STAKE | value **0.05**; balance 0.178982392 → 0.128591456; sendMode=3 |
| paidMask | **1** |
| CANCEL | `6qfclhvNVLi5AycYvH7z75IkHU0ZUo55WOVK+6O+7uI=` @ 0.02 (oracle seqno advanced) |
| refundMask | **0** (on-chain re-read) |
| refundedTotal | **0** |
| remainingBalanceTon | 0.105737315 (stake still held) |
| Note | Harness initially logged `success=true` on CANCEL broadcast only; V.2D.2 classification uses on-chain refundMask |

### 0.02 / C

| Classification | **NOT RUN** |
| Reason | 0.02/B refund lifecycle failed |

### Updated matrix status

| ValueTon | A | B | C | Level |
|---------:|:-:|:-:|:-:|:------|
| 0.05 | PASS | PASS | PASS | PASS |
| 0.04 | PASS | PASS | PASS | PASS |
| 0.03 | PASS | PASS | PASS | PASS |
| **0.025** | PASS | **PASS** (V.2D.2) | PASS | **PASS (fully validated)** |
| **0.02** | PASS | **VALUETON_FAILURE** | NOT RUN | **FAIL (cancel/refund bound)** |

### Minimum fully validated ValueTon

```text
0.025 TON
```

### Should 0.015 be tested next?

```text
NO
```

0.02 already fails Scenario B cancel/refund under `CANCEL_GAS_RESERVE=0.08`. Lower values are out of scope until cancel-attach coupling is redesigned or measured separately.

---

## R17.8V.2D.1 Reduced Matrix Sweep

**Execution date:** 2026-08-15  
**Network:** testnet (`https://testnet.toncenter.com/api/v2/jsonRPC`)  
**Stake Ton (player):** 0.05 (unchanged)  
**Oracle ValueTon matrix:** `0.05, 0.04, 0.03, 0.025, 0.02`  
**Scenarios:** A, B, C  
**Start balance (operator):** 4.788753761 TON  
**Launchers:** `run-v2d1-matrix-live.js` → resume `run-v2d1-resume-from-004.js` → RPC retries for 0.025  
**Artifacts:** `server/tests/testnet/artifacts/valueton-sweep-*.json` (gitignored)  
**Production lifecycle / GameEscrow / Payment / Recovery / defaults:** **unchanged**

### Decision rule

ValueTon level **PASS** only if A ∧ B ∧ C all PASS.  
Infra/RPC failures are recorded separately and **do not** establish a ValueTon gas floor.

### Matrix results

| ValueTon | A | B | C | Level result | Notes |
|---------:|---|---|---|--------------|-------|
| 0.05 | PASS | PASS | PASS | **PASS** | First run |
| 0.04 | PASS | PASS | PASS | **PASS** | First attempt RPC outage; **resume PASS** |
| 0.03 | PASS | PASS | PASS | **PASS** | Resume run |
| 0.025 | PASS | FAIL* | PASS† | **INCOMPLETE** | *RPC/timeout/seqno-33 flakes; †C PASS on retry |
| 0.02 | PASS | SKIP | SKIP | **INCOMPLETE** | B/C skipped after B marked failed at 0.025 |

### Minimum fully passing ValueTon

```
0.03 TON
```

(A+B+C all confirmed on testnet.)

### Failure threshold (ValueTon gas)

```
NOT ESTABLISHED
```

0.025 and 0.02 were **not** proven insufficient. Observed failures were TonCenter `fetch failed` / timeout / HTTP 500 / wallet `exitcode=33` (stale seqno retry). Scenario A alone still completed at **0.02**.

### Approximate TON spent

| Item | TON |
|------|----:|
| Start balance | 4.788753761 |
| End balance | 0.182782788 |
| **Spent (measured)** | **4.605971** |

Includes player funding, oracle attaches across retries, and RPC-retry waste. Per-message fees were not isolated.

### Scenario detail — 0.05 (PASS)

| Sc | Contract | refundMask | CANCEL tx |
|----|----------|------------|-----------|
| A | `EQDH4mS3XEWMHCnF0_2NG5Y7VqWvucznP4c-1CIlh_Px6-rh` | 0 | `ixcqOe16l8DvVSHhhJdRBZCA2+9Mf0elQc22zhdj2Rg=` |
| B | `EQCNFdOpE1wKlz5tA0NgVtYeqBYi7NIEDHGaKJql2E8BAAeG` | 1 | `W405L5tkgCmxQIyeA9rw4oJBuo2k2AB17WBeFtYbUOY=` |
| C | `EQBuoq1FsMClNRRq3RHjgaw6wiGs90vuYb9FoF1JJdB4PIKF` | 7 | `xBb09aNcuOHpxe0Bmw07HHNLuT3Ivs2WTwOh3hr9MQk=` |

### Scenario detail — 0.04 (PASS after resume)

| Sc | Contract | CANCEL tx |
|----|----------|-----------|
| A | `EQCZgZlBCFG6j5u20Qa4HdkUz67LThYm8fL97hKM3LEZt-rr` | `0Pz3pGYugew5/e2txJDLVTuiR/9Wp9jLRsaS91Orxfg=` |
| B | `EQAiSnuFdxFd2cGrqyj8nxc1wjeonCDybK6jOGCceVmnbu2h` | `u5G0KLcQQohD2KL1Wxuc0Ip8TeOoLgfvpd75ZMzKAww=` |
| C | `EQDDu9wzWnGclYsnVHx6SNHf2sKPp6Srxt7Yz8qaeyIaB3Zq` | `wwFVO3rMDdP71ev8ZgU/qNF22+Trm50AABH7FJXEJvU=` |

### Scenario detail — 0.03 (PASS)

| Sc | Contract | CANCEL tx |
|----|----------|-----------|
| A | `EQD7nld7JZk1IqgJErzheVZJlRhwJOA5ubK3DHaoePHH_fnG` | `dvDU1TIBO+yxstDeAMPcGHXcjJDIRa9F8PH0ectSbBc=` |
| B | `EQDboKoclJ4Id5o5P4_Rkl0e3pO6G3qCx1kX3pyjWi_r5eHo` | `qJ3DUo/dBAMRUVFBQM9DxKx2HQaomf+3X5WTrx0smiU=` |
| C | `EQCsYJ51Wu-EeWUM1DNT3s8w23qnak_ty-Fr47T2EIGQJb6E` | `9e6cnd4QYiOgD58l1SyEA18xD3z9HmkCyPACiD2ewgU=` |

### Scenario detail — 0.025 / 0.02 (incomplete)

| Value | Sc | Result | Notes |
|------:|----|--------|-------|
| 0.025 | A | PASS | `EQDG9zcAojUEnnqox4tbD0vbT_YLiOqXZ7RDgYaLVi3VCRHV` |
| 0.025 | B | FAIL (infra) | timeouts / HTTP 500 / exitcode 33 — not ValueTon |
| 0.025 | C | PASS (retry) | `EQCDNW_tT-UFU-sBAdL3NwxX6zd7XvV9_WjXIQYeEqCchPBx` paidMask=7 |
| 0.02 | A | PASS | `EQCjE2GcomsmlANnkTFmiTat0zAMAuDA2Ksc02v3c50M5AZh` |
| 0.02 | B/C | NOT RUN | skipped after B failure mark |

### Next measurement step (not this commit)

Re-run **B @ 0.025** then **B+C @ 0.02** when TonCenter is stable and deployer is re-funded — to discover whether ValueTon gas floor is below 0.03.

---

## R17.8V.2C Player Wallet Initialization Fix

**Date:** 2026-08-15  
**Network:** testnet  
**Scope:** harness only — Scenario B + C @ **0.05** (Scenario A already PASS in V.2B)  
**Launcher:** `node server/scripts/run-v2c-bc-live.js`  
**Artifact:** `server/tests/testnet/artifacts/valueton-sweep-last-run.json`  
**Production lifecycle / GameEscrow / Payment / Recovery:** **unchanged**

### Root cause

1. **Primary (V.2B):** ephemeral player wallets were **funded but not deployed**. Wallet V4 `getSeqno` on uninitialized accounts → exit **-13**; STAKE never reached.
2. **Secondary (first V.2C attempt):** after StateInit deploy, STAKE used default wallet `sendMode = NONE (0)`, so forward fees were taken from the attach. GameEscrow requires `context().value == required` exactly → STAKE **bounced**, `paidMask` stayed 0.

### Fix (Option A)

Harness-only changes in `server/tests/testnet/runValueTonSweep.js`:

- Deploy ephemeral Wallet V4 with **StateInit** before STAKE; wait until **active** and `seqno >= 1`.
- Refuse STAKE if wallet is not active.
- Use `SendMode.PAY_GAS_SEPARATELY | IGNORE_ERRORS` (mode 3) so exact stake amount arrives.
- Wait for player seqno advance after STAKE; poll `paidMask`.
- Test-only logs: address, active status, deployment status, balance before/after stake (no secrets).

### Validation result

| Scenario | Result | Contract | paidMask | refundMask |
|----------|--------|----------|---------:|-----------:|
| B — 1 STAKE → CANCEL → REFUND | **PASS** | `EQBmf8YbFRApIj_8qe_5-nYWnDRRPgBnUb1Q4XTlXoXEreKU` | 1 | 1 |
| C — 3 STAKE → CANCEL → 3 REFUNDS | **PASS** | `EQBVbOJBOCKDV3mjT4vF38I4Lq_r4ZuJ0x4ZMY8AXoWkHGDo` | 7 | 7 |

Notes: post-cancel `get_status` may still return exit **11** (same as V.2B A); cancel + `refundMask` are authoritative for this harness. No stuck escrow / no REFUND_PENDING observed. Sweep fee measurements not added in this stage.

### Scenario B — key steps

| Step | Tx / note |
|------|-----------|
| DEPLOY | `YUf5InI5n5H8QYpNDQmyZfV+C1yydOKNcctI/0Fzs+g=` |
| INIT_GAME | `Cr+vdyN5wYsyWhOaXHvp+kxE335jRwnqG4XS/TKAZX4=` |
| OPEN_PAYMENTS | `HrZVeceW3uD/W01rcJ0JtgN4kscFXMwjzYZeEIQxSpo=` |
| PLAYER_WALLET_DEPLOY | active `EQBcT94ir9ikXQ8eCf7KqgcxlYZMekCNvnT2mQ3tNHv6CjJu` |
| STAKE | balance 0.129→0.078; paidMask=1 |
| CANCEL | `C+f7R5kz9YrG3wAPJhZKOw8KptYTdBDZSP6Xrq32cRY=` |

### Scenario C — key steps

| Step | Tx / note |
|------|-----------|
| DEPLOY | `+VVzrihcyqEqFmFPSywZXb5wwqaBHcvRWL4jzjxad50=` |
| INIT_GAME | `oUB/yqGI0Ub3sH3ipELPsEoM87i2kaDgBXPFOE4EdOw=` |
| OPEN_PAYMENTS | `AAFBvOCQZBXYhYTk2NHXa4Kex1htgdcsr1YwT4EordA=` |
| 3× STAKE | paidMask=7 |
| CANCEL | `W6O3B2jWOrGhFliBYp9mylyC4rJANhecq/YsTFAXM3A=` ; refundMask=7 |

---

## R17.8V.2B Baseline Execution

**Execution date:** 2026-08-15  
**Network:** testnet (`https://testnet.toncenter.com/api/v2/jsonRPC`)  
**Baseline valueTon:** **0.05** (all oracle messages)  
**Stake Ton:** 0.05  
**Deployer (masked):** `EQB83s....3PDQ`  
**Preflight balance:** 8.438956186 TON  
**Artifact:** `server/tests/testnet/artifacts/valueton-sweep-last-run.json`  
**Launcher:** `node server/scripts/run-v2b-baseline-live.js` (runtime env only; `.env` not permanently modified)

### Scenario results

| Scenario | Result | Contract | Notes |
|----------|--------|----------|-------|
| A — 0 paid cancel | **PASS** | `EQDheBHUWh5aDJYaJAjNhsYoFYw4jSirBLv4NR_umrNwk164` | DEPLOY→INIT→OPEN→CANCEL all ok |
| B — 1 paid cancel | **FAIL** | `EQCbmu4iJAgZ-3ub3RHHZaCoBZu5zryMBETXcJBt_qSUP6yq` | Failed after OPEN; no STAKE/CANCEL step recorded |
| C — 3 paid cancel | **FAIL** | `EQBTFXz75GUS32vN8wDWvg9wMooLJZ-cymhPF-9NSc8tYZ3K` | Same failure class as B |

**V.2B overall: FAIL** (requires A+B+C PASS).

### Scenario A — transaction references

| Step | Tx hash | valueTon |
|------|---------|----------|
| DEPLOY | `XeGNYqqIlLsHg3DbnesfmshaQWgoRuctVdq3c9o9/Ec=` | 0.05 |
| INIT_GAME | `HIVwvyEc3KTRDqRe/0XJyZuPCncZV4enBbSrYUiiLac=` | 0.05 |
| OPEN_PAYMENTS | `IQPP/ZrFjd30Bi9CquZv7MZx0RXE2RoPzcebmUG4IKA=` | 0.05 |
| CANCEL | `GM2fw2ELPshsvztqbGbjfibtDHaVUUKgH5cW8kHbr+I=` | 0.05 |

Post-cancel: `remainingBalanceTon ≈ 0.1956` (attach residual; 0 stakes to refund).  
`get_status` after cancel returned exit_code **11** (getter/decode issue; cancel broadcast still confirmed).

### Scenario B — failure

| Step | Result |
|------|--------|
| DEPLOY | PASS `EIQEAgbSwtjNj/TLbQC1f9Oyimjt0Sbu7pZanL99/e0=` |
| INIT_GAME | PASS `9vulknAAxzYsikkerFtmuEL9uIZJCKRfW9q9//f5Ezo=` |
| OPEN_PAYMENTS | PASS `l6lfJryB6Y6Amxm3veWjdY1GoeF/6vTDCQiX1L28zqU=` |
| STAKE / CANCEL | **NOT REACHED** |
| Error | `Unable to execute get method. Got exit_code: -13` |

### Scenario C — failure

| Step | Result |
|------|--------|
| DEPLOY | PASS `VXaje/Hdoz7Ff/xzWyWy4X3gDgUraGt8r7Mr6sHoSHI=` |
| INIT_GAME | PASS `n4cEf4/S0Xd8SKgFZeC/Eh9u0JZmPUMqXgDqWhOG5QY=` |
| OPEN_PAYMENTS | PASS `OhCNFlP7srWjRBV4Det1yy6s+1qcB5+U61o3sJ4U+ls=` |
| STAKE / CANCEL | **NOT REACHED** |
| Error | `Unable to execute get method. Got exit_code: -13` |

### Investigation required (blocker for B/C)

Harness funds **ephemeral player addresses** then sends STAKE via `WalletContractV4` external messages. Exit **-13** on get-method/`getSeqno` is consistent with **uninitialized player wallet contracts** (fund transfer does not deploy V4 wallet StateInit).  

**Required fix before retry:** player STAKE sender must include wallet `init` on first outbound message (or use pre-deployed test wallets).  

**Do not** proceed to lower `valueTon` until B and C PASS.

### Approximate TON spent (deployer)

From preflight **8.438956186** TON at start of A to **7.682016408** TON at start of C deploy ≈ **0.757 TON** spent through A+B oracle path + B funding (exact fee split not metered). Additional C DEPLOY+INIT+OPEN attach after that. Not a precise fee table.

---

## R17.8V.2A Environment Audit

**Audit date:** 2026-08-15  
**Tool:** `node server/scripts/check-testnet-valueton-env.js` (read-only; no broadcasts)  
**Dry-run:** `node server/scripts/run-testnet-valueton-sweep.js --dry-run` → **PASS**

### Environment readiness

| Check | Result |
|-------|--------|
| Overall | **READY** |
| `server/.env` | FOUND |
| `server/.env.example` | FOUND |
| `TON_NETWORK` | FOUND → `testnet` |
| `TON_DEPLOY_MODE` | FOUND → `live` |
| Mainnet enabled | NO |
| Deployer mnemonic configured | YES (secret not logged) |
| Address derivation | PASS (`WalletContractV4R2`, masked `EQB83s....3PDQ`) |
| Override default path | Resolves to **0.05** when override disabled |
| `TEST_VALUETON_*` in `.env` | MISSING (expected — set at live-run time) |

### Balance estimates (not live-queried)

| Scenario | Approx. cost (TON) |
|----------|-------------------:|
| A — DEPLOY+INIT+OPEN+CANCEL | ~0.25 |
| B — +1 stake + fund gas | ~0.35 |
| C — +3 stakes + fund gas | ~0.55 |
| One full A+B+C pass | ~1.15 |
| **Minimum recommended testnet balance** | **~4.5 TON** (≈3× pass + margin for sweep retries) |

### Missing / optional before live sweep

- Set runtime flags for live run (`RUN_TESTNET_VALUETON_SWEEP`, `TEST_VALUETON_OVERRIDE`, sweep values) — not required in `.env` permanently.
- Confirm deployer **on-chain** balance ≥ ~4.5 TON (V.2A did not query chain).
- `TON_ENDPOINT` unset → profile default testnet RPC (OK if reachable).
- `GAME_ESCROW_MODE` unset in `.env` → testnet profile default `game` (OK).

### Blockers before live sweep

**None for tooling/credentials on this machine.**  
Remaining operator step: fund/check balance, then run live sweep explicitly (separate task).

---

## How to run

### Dry-run (no chain)

```bash
node server/scripts/run-testnet-valueton-sweep.js --dry-run
```

### Live testnet sweep

```bash
# server/.env must provide TON_DEPLOYER_MNEMONIC and testnet endpoint/key
set TON_NETWORK=testnet
set TON_DEPLOY_MODE=live
set RUN_TESTNET_VALUETON_SWEEP=true
set TEST_VALUETON_OVERRIDE=true
set TEST_VALUETON_SWEEP_VALUES=0.05,0.04,0.03,0.025,0.02,0.015,0.01
set TEST_VALUETON_SCENARIOS=A,B,C
set TEST_VALUETON_STAKE_TON=0.05
node server/scripts/run-testnet-valueton-sweep.js
```

Optional per-message overrides (used by adapter when override flag is true):

```text
TEST_VALUETON_DEPLOY
TEST_VALUETON_INIT
TEST_VALUETON_OPEN
TEST_VALUETON_CANCEL
TEST_VALUETON_SETTLE
```

Live runs write JSON to:

```text
server/tests/testnet/artifacts/valueton-sweep-last-run.json
```

Then paste outcomes into the tables below.

---

## Configured baseline (production)

| Message | Current `valueTon` |
|---------|-------------------:|
| DEPLOY | 0.05 |
| INIT_GAME | 0.05 |
| OPEN_PAYMENTS | 0.05 |
| CANCEL | 0.05 |
| SETTLE | 0.05 |

Failed lifecycle attach sum: `0.05 × 4 = 0.20` (+ fees ≈ **0.223** historically).

Contract gates: `CANCEL_GAS_RESERVE = 0.08`, `SETTLE_GAS_RESERVE = 0.05`.

---

## Live results table (fill after sweep)

| Message | Value TON | Success | Fee | Exit Code | Notes |
|---|---:|---|---|---|---|
| DEPLOY / INIT / OPEN / CANCEL | 0.025 | YES | — | 0 | V.2D.2 A+B+C fully validated |
| DEPLOY / INIT / OPEN / CANCEL | 0.02 | NO (B refund) | — | — | VALUETON_FAILURE cancel vs CANCEL_GAS_RESERVE |
| SETTLE | … | — | — | — | not in V.2D.2 scope |

### Summary recommendation table

| Message | Minimum fully passing (A+B+C) | Recommended production value | Actual fee | Failure threshold |
|---|---|---|---|---|
| DEPLOY | **0.025** | **0.05 KEEP** | not isolated | B cancel/refund fails @ **0.02** |
| INIT_GAME | **0.025** | **0.05 KEEP** | not isolated | same |
| OPEN_PAYMENTS | **0.025** | **0.05 KEEP** | not isolated | same |
| CANCEL | **0.025** | **0.05 KEEP** | not isolated | coupled to `CANCEL_GAS_RESERVE=0.08` |
| SETTLE | NOT MEASURED | **0.05 KEEP** | — | — |

Production remains **0.05**. Do not lower production; `recommended = minimum_successful × 2` → 0.05 exactly at the floor of that rule for 0.025.

### Scenario results

| Scenario | Result |
|---|---|
| No payment cancel (A) | **PASS** through **0.02** (V.2D.1) |
| One player refund (B) | **PASS** through **0.025** (V.2D.2); **FAIL @ 0.02** (refund) |
| Three player refund (C) | **PASS** through **0.025**; **0.02 NOT RUN** |

---

## R17.8V.1C … V.2E RESULT

```
Production defaults: 0.05 (unchanged; audit only)
V.2D.2: 0.025 fully validated; 0.02/B VALUETON_FAILURE (cancel/refund)
V.2E: CANCEL gate = myBalance() >= pendingRefund + 0.08
      Uniform V floor ≈ (0.08 + G)/4 ≈ 0.021 (G≈0.004 from live 0.02/B)
      0.025 = confirmed live minimum; 0.022–0.024 theoretically useful next tests
Conclusion: B — lower boundary exists; do not change production 0.05
```
