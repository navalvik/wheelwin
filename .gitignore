#
# R7.69E - local investigation scratch (never commit)
_audit_tmp/

# P6.8A — owner production wallet (created manually after deployment).
config/owner.json

# R8.0B — generated release packages (build via npm run release:build)
/release/

# R8.0C — generated certification outputs (via npm run release:certify)
docs/release/Release-Certification-Report.md
docs/release/ReleaseCertificate.json

# R6.1 — persisted application environment state (admin dashboard)
server/runtime/

# R7.0 — immutable session lifecycle archives
/data/session-history/
server/data/session-history/

# R8.10 — durable TON financial persistence (runtime)
server/data/ton-financial/

# R13.9F — forensic archive staging (local ZIP before GCS upload)
data/archive-staging/
server/data/archive-staging/
