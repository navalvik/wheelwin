# R17.9T.8 — Attack E Observability — Operationally Verified (Final Production Evidence)

Date: 2026-08-24

Task: documentation-only checkpoint of the completed R17.9T.8 milestone using the latest real production evidence. No source code, tests, configuration, monitoring calculations, or Developer Console changes were made.

## 1. Milestone

R17.9T.8 — Attack E Observability — **Operationally Verified**.

## 2. Objective

Passive observability of the theoretical Attack E room-pool saturation risk (many distinct legitimate Telegram identities → many active rooms → global 64-room pool saturation). No enforcement controls were in scope.

## 3. Production Topology

- Vercel: static React frontend + Developer Console (`/debug`, SYSTEM → Metrics).
- Railway (project invigorating-growth, service wheelwin): Node production backend serving `/health`; console projections delivered over the existing socket path.
- Monitoring architecture unchanged: GameplayMetricsCollector → MetricsRegistry → MetricsSnapshot → console projection / exporters. `prometheusEnabled` remains `false` (untouched); console is the observability surface.

## 4. Production Validation (real gameplay activity, manually observed)

Manually inspected at https://wheelwin-nine.vercel.app/debug → SYSTEM → Metrics after three rooms were created from three different Telegram Android devices/accounts:

- ACTIVE PLAYERS = 3
- GAMES = 0
- RECONNECTS = 0
- Room pool max = 64
- Room pool utilization = 5%
- Near capacity = no
- Rooms created / min = 0 (at the observed sampling moment)
- Limit rejections / min = 0
- Counters → rooms.created = 3
- No saturation observed; no ROOM_CREATION_LIMIT rejections observed.

Verified fact: three different Telegram Android clients/accounts created three rooms. The three Telegram numeric IDs were NOT separately verified in production logs and are deliberately not claimed or invented.

## 5. Utilization Calculation

3 / 64 = 0.046875 → displayed as **5%** (percentage rounding), consistent with the implementation's formatting.

## 6. Observability Signals Verified Operational

- Room pool maximum ✔ (64)
- Room pool utilization ✔ (5%)
- Near-capacity state ✔ ("no")
- CREATE_ROOM velocity ✔ (0/min at sampling moment)
- Room-limit rejection velocity ✔ (0/min)
- rooms.created counter ✔ (= 3, authoritative total creation counter)

Presentation note on the two "total" rows: the Room pool table's total rows may render as "—" while the underlying counters exist — the authoritative observed total creation counter in the console Counters section is `rooms.created = 3`. This is a display/presentation behavior distinction, not evidence of missing counter functionality; any further presentation adjustment would be a separate scoped task with its own evidence.

## 7. Security Scope Confirmation

R17.9T.8 introduced NONE of the following: rate limiting, cooldowns, CAPTCHA, account-age restrictions, IP restrictions, device fingerprinting, reputation systems, Telegram authentication changes, CREATE_ROOM authorization changes, one-room-per-user quota changes, global room cap changes, room lifecycle changes, recovery changes, gameplay changes, financial changes.

## 8. Attack E Conclusion

Attack E remains a theoretically possible room-pool saturation scenario, but no production evidence currently demonstrates active abuse or saturation. R17.9T.8 provides the required passive observability to detect such conditions if they occur. Therefore no Anti-Abuse mitigation is justified by current evidence.

## 9. R17.9T.9 — DEFERRED

A future Anti-Abuse stage should only be opened if production evidence demonstrates a meaningful abuse pattern (e.g., sustained near-capacity utilization, elevated limit-rejection velocity) or if a separate security requirement establishes the need. Not designed or implemented here.

## 10. Commit Chain (all pushed to origin/main)

1. `7a21121fc04158deccf2f94b0fa4ca3ce90db9da` — Add R17.9T.8 Attack E observability
2. `a39fe4224a3e0473cfdd4ab366b1fd35335e5afd` — Complete R17.9T.8 console gauge visibility
3. `770c427fe0b173d0e7f1d177483b8beda93db226` — Fix R17.9T.8 metrics console row contract

Authoritative reports by chronology (unchanged): initial implementation → `2026-08-24_r17.9T.8_attack_e_observability.md`; console visibility completion → `2026-08-24_r17.9T.8_console_gauge_visibility_completion.md`; row-contract fix → `2026-08-24_r17.9T.8_metrics_console_row_contract_fix.md`; earlier access-limited validations → `..._production_observability_validation.md` / `..._backend_runtime_metrics_validation.md`; this file is the final production evidence checkpoint.

## 11. Checkpoint Scope

This task changed documentation only: this single new report file was staged and committed (`Document R17.9T.8 production verification`). Working tree contained no modified tracked files before staging.
