# R17.8G — Real Device Recovery Validation Record

**Mode:** Documentation only (no application code changes)  
**Date:** 2026-08-14  
**Depends on:** R17.8A–F (recovery resilience investigation + RoomLobby reconnect fix)  
**Verdict:** **R17.8F Real Device Validation PASSED**

---

## 1. Purpose

Permanent record of real-device validation for mobile reconnect / RoomLobby recovery after:

| Stage | Title |
|-------|--------|
| R17.8A | Recovery Snapshot Availability Audit |
| R17.8B | Payment-phase Recovery Success Path |
| R17.8C | Gameplay Recovery Snapshot Validation |
| R17.8D | Recovery Integration Validation |
| R17.8E | RoomLobby Reconnect State Audit |
| R17.8F | RoomLobby Reconnect State Machine Fix |

This stage does **not** change architecture or application code. It records observed production-like device behavior after R17.8F.

---

## 2. Test environment

| Item | Value |
|------|--------|
| Platform | Android (3 devices) |
| Network | Wi‑Fi |
| Fault injection | Intentional Wi‑Fi interruption, then restoration |
| Application stage | **RoomLobby** |
| Game room | **Not created** (pre-join / waiting lobby only) |
| Focus | Client reconnect UI + transport recovery (not gameplay snapshot recovery) |

---

## 3. Devices and clients

| Device | Client |
|--------|--------|
| Device 1 | Telegram WebApp / Bot |
| Device 2 | Telegram WebApp / Bot |
| Device 3 | Opera Browser |

---

## 4. Test procedure

For each Android device:

```text
Open WheelWin
  ↓
Enter RoomLobby
  ↓
Disable Wi‑Fi
  ↓
Observe reconnect behavior
  ↓
Restore Wi‑Fi
  ↓
Observe recovery
  ↓
Verify RoomLobby state
  ↓
Verify advertising synchronization
```

Expected lifecycle:

```text
disconnect
  ↓
RECONNECTING
  ↓
Wi‑Fi restored
  ↓
socket reconnect
  ↓
RoomLobby restored
```

---

## 5. Observed results

### 5.1 Device recovery

| Check | Result |
|-------|--------|
| All three devices recovered after Wi‑Fi restore | **PASS** |
| Return to normal RoomLobby (not stuck on Reconnecting) | **PASS** |

### 5.2 Client types

#### Telegram WebApp (Devices 1–2)

| Check | Result |
|-------|--------|
| Disconnect handling | **PASS** |
| Reconnect | **PASS** |
| Return to RoomLobby | **PASS** |
| Advertising reconnection | **PASS** |

#### Opera Browser (Device 3)

| Check | Result |
|-------|--------|
| Disconnect handling | **PASS** |
| Reconnect | **PASS** |
| Return to RoomLobby | **PASS** |
| Advertising reconnection | **PASS** |

### 5.3 Advertising validation

| Observation | Result |
|-------------|--------|
| Advertising banners continued switching synchronously | **PASS** |
| Advertisement sync not broken by reconnect | **PASS** |
| RoomLobby reconnect state compatible with ad transport | **PASS** |

### 5.4 Regression scope (observation)

No gameplay, payment, TON, or server recovery regressions were observed during this RoomLobby-only test.

---

## 6. Final validation statement

```text
R17.8F Real Device Validation PASSED

Three Android devices were tested:

* two Telegram WebApp clients;
* one Opera Browser client.

All devices successfully recovered from WiFi interruption and returned to RoomLobby.

Advertising synchronization remained functional.

No gameplay, payment, TON, or server recovery regressions observed.
```

---

## 7. Traceability

| Artifact | Role |
|----------|------|
| `docs/R17.8A_RECOVERY_SNAPSHOT_AUDIT.md` | Snapshot availability gap |
| `docs/R17.8C_GAMEPLAY_RECOVERY_SNAPSHOT_VALIDATION.md` | Post-`GAME_INITIALIZED` safety |
| `docs/R17.8E_ROOMLOBBY_RECONNECT_AUDIT.md` | Stuck Reconnecting root cause + R17.8F fix notes |
| `docs/R17.8G_REAL_DEVICE_RECOVERY_VALIDATION.md` | This real-device PASS record |
| Commit `e2694aa` | R17.8F RoomLobby reconnect state machine fix |

---

## 8. Restrictions confirmed

This document stage did **not** modify:

- frontend or backend application logic;
- RecoveryEngine / SocketGateway;
- Advertisement Engine;
- Gameplay / Physics / Payment Engines;
- TON integration.
