# R8.0B — Release Validation Report

**Stage:** Release Candidate Build System & Release Artifacts  
**Date:** 2026-07-25  
**Scope:** Reproducible release packaging, manifests, checksums, fingerprints (no gameplay / infrastructure redesign)

---

## Deliverables

| # | Deliverable | Status | Location |
|---|-------------|--------|----------|
| 1 | ReleaseManager | Done | `server/release/ReleaseManager.js` |
| 2 | ReleaseBuilder | Done | `server/release/ReleaseBuilder.js` |
| 3 | ReleaseManifest | Done | `ReleaseManifestBuilder.js` → `manifests/ReleaseManifest.json` |
| 4 | ChecksumGenerator | Done | `server/release/ChecksumGenerator.js` |
| 5 | BuildFingerprint | Done | `server/release/BuildFingerprint.js` |
| 6 | ReleaseNotesGenerator | Done | `server/release/ReleaseNotesGenerator.js` |
| 7 | Artifact packaging | Done | `ReleaseArtifactManager.js` |
| 8 | Release validation | Done | `ReleaseIntegrityVerifier.js` |
| 9 | Developer Console | Done | Server Health additive cards |
| 10 | Validation report | Done | This document |

---

## Pipeline

```text
Source → Build Validation → Client Package → Server Package
  → Artifact Packaging → Checksums → Manifest → Verification → Release Package
```

CLI: `npm run release:build` (`server/scripts/run-release-build.js`)

Output layout:

```text
release/wheelwin-{version}-{channel}/
  server/
  client/
  documentation/
  manifests/ReleaseManifest.json
  manifests/ReleaseMetadata.json
  checksums/SHA256SUMS
  release-notes/RELEASE_NOTES.md
```

---

## Channels

`development` | `internal` | `rc` | `beta` | `production`

---

## Configuration

`RELEASE_CHANNEL`, `RELEASE_OUTPUT_DIRECTORY`, `RELEASE_SIGNING_ENABLED`, `RELEASE_GENERATE_CHECKSUMS`, `RELEASE_INCLUDE_DOCS`, `RELEASE_INCLUDE_REPORTS`

Validated by ConfigurationManager. Signing flag reserved (checksums are the integrity mechanism in R8.0B).

---

## Observability

- **Logging:** release started / artifacts / manifest / checksums / verified / completed  
- **Health / Console:** version, channel, commit, builtAt, fingerprint, status (no absolute paths)  
- **Monitoring:** `ReleaseMetricsCollector` channel + verified gauges  

---

## Tests (`releaseBuild.test.js`)

| Case | Result |
|------|--------|
| SemVer validation | Pass |
| Checksum + fingerprint determinism | Pass |
| Manifest + notes generation | Pass |
| Artifact packaging + integrity verify | Pass |
| Reproducible fingerprint across two builds | Pass |

---

## Constraints checklist

| Constraint | Met |
|------------|-----|
| No gameplay redesign | Yes |
| No infrastructure redesign | Yes |
| No deployment redesign | Yes (additive release packaging only) |
| Console protocol unchanged | Yes (additive DTO fields) |

---

## Success criteria

- Reproducible packages — **Yes** (stable artifact contents + deterministic fingerprint)  
- Every artifact verified — **Yes**  
- Deterministic checksums — **Yes**  
- Complete manifest — **Yes**  
- Unique build fingerprint — **Yes**  
- Gameplay / infrastructure unchanged — **Yes**
