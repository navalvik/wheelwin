# R7.69E — Safe Codebase Cleanup Audit

**Date:** 2026-08-07  
**Branch:** `main`  
**Scope:** Conservative dead/obsolete cleanup only. Zero intended behavior change.  
**Basis:** [R7.69D-GameEscrow-Production-Audit.md](./R7.69D-GameEscrow-Production-Audit.md) V4 / production classifications.  
**Policy:** Prefer under-cleaning over risk. Do not touch GameEscrow paths, STAKE/INIT/SETTLE/CANCEL/refunds, BlockchainMonitor, PaymentSessionManager, ContractSettlementManager, TonConnect, Page4/5/6, Winner Engine, network config, ABIs, or protocols.

---

## Summary counts

| Bucket | Count |
|--------|------:|
| Safe to remove now | 44 path groups (39 `_audit_tmp` + 2 `.bak` + 2 investigation scripts + gitignore rule) |
| Keep | 12+ categories (see below) |
| Deferred | 6+ categories |

---

## Safe to remove now

Evidence: no runtime/`package.json`/doc references; scratch or backup only; deletion cannot affect GameEscrow / V4 rollback / Mainnet readiness.

| Item | Evidence | Action |
|------|----------|--------|
| Tracked `_audit_tmp/**` (39 files: curl dumps, live JS copies, patch helpers, vercel/railway probe artifacts) | Investigation scratch; not imported by app; committed by mistake under R7.20C-era work | Delete from repo; ignore `_audit_tmp/` |
| Untracked `_audit_tmp/*.txt` leftovers | Local log dumps; never commit | Delete from working tree only |
| `client/src/components/ModeSelector.jsx.bak` | Backup only; no `ModeSelector.jsx` live file; zero imports of ModeSelector | Delete |
| `client/src/styles/modeSelector.css.bak` | Backup only; no live `modeSelector.css`; zero references | Delete |
| `server/scripts/r741_derive_deployer_address.mjs` | Header: “R7.41 temporary diagnostic”; not in `package.json`; no docs/code refs | Delete |
| `server/scripts/r744_verify_gram_mnemonic.mjs` | One-off mnemonic MATCH check; not in `package.json`; no docs/code refs | Delete |
| `.gitignore` entry `_audit_tmp/` | Prevent re-commit of scratch | Add |

**Not in this pass:** unused import sweeps, comment churn inside production modules, dependency removals (all listed packages appear used).

---

## Keep (required / temporary compatibility / rollback / Mainnet safety)

Aligned with R7.69D Phase A and cutover notes:

| Item | Classification | Why keep |
|------|----------------|----------|
| `WalletContractV4` deployer/oracle signing | **required** | Not escrow; oracle identity |
| `buildV4EscrowWallet` / `serializeLegacySettleBody` / V4 settle confirm path | **temporary compatibility** | Active when `GAME_ESCROW_MODE=v4` rollback |
| Mainnet profile default `gameEscrowMode=v4` + readiness gate | **temporary compatibility** | R7.68 staged Mainnet safety |
| GameEscrow contract, ABIs, compile/deploy artifacts | **required** | Production escrow |
| `BlockchainMonitor`, `PaymentSessionManager`, `ContractSettlementManager` | **required** | Payment/settle/refund authority |
| TonConnect / Page4/5/6 / Winner Engine | **required** | Product surface (out of scope) |
| `server/scripts/check-mainnet-readiness.js` | **required** | Mainnet ops gate |
| `server/scripts/run-game-escrow-e2e.js`, `run-game-escrow-soak.js`, production/release reports | **required** | Validation / certification |
| `server/scripts/r748_activate_deploy_wallet.mjs` | **keep (ops)** | One-time deploy-wallet activation; treat as deploy/ops tooling — do not remove under “scratch” rule |
| `TonMainnetReadiness`, `TonWalletIdentityDebug`, DeployPipelineForensics | **required** | Production diagnostics |
| Test fixtures using `WalletContractV4` | **required** | Tests, not production escrow |
| Comment-based TonConnect payload fallback | **temporary compatibility** | Client rollback path |

---

## Deferred (needs more proof)

| Item | Why deferred |
|------|----------------|
| Broad unused-import / dead-export pass across `server/` and `client/` | Needs automated unused analysis; high false-positive risk near payment/settlement |
| Removing or merging V4 escrow helpers while Mainnet default remains `v4` | Explicitly temporary compatibility until cutover (R7.69D W7 / cutover plan) |
| BlockchainMonitor terminal-watch prune (R7.69D W2) | Behavior/lifecycle change — not cleanup |
| Settlement/session Map eviction (W3/W4) | Behavior change |
| Snapshot `oracleWallet` freeze (W1) / manager constructor cleanup (W8) | Feature/hardening, not dead code |
| Dependency upgrades or package removals without usage proof | Hard constraint: no upgrades; only remove if proven unused (none proven this pass) |
| Other historical `server/scripts/r7*` if any reappear | Classify per script; readiness/deploy/e2e stay |

---

## Applied in this commit

1. Removed tracked `_audit_tmp/**` investigation artifacts; added `_audit_tmp/` to `.gitignore`.
2. Removed `.bak` ModeSelector component/style leftovers.
3. Removed investigation-only scripts `r741_derive_deployer_address.mjs`, `r744_verify_gram_mnemonic.mjs`.
4. **Restored / kept** `r748_activate_deploy_wallet.mjs` (ops deploy tooling).
5. No changes to GameEscrow, monitors, settlement, TonConnect, pages, Winner Engine, config, ABIs, or dependencies.

---

## Validation

Focused GameEscrow/payment subset (14 files): 13 passed.  
`gameContract.manager.test.js` fails with pre-existing `this._logger.decisionTrace is not a function` (untouched by this cleanup; not a regression from removals).

---

## Related

- [R7.69D-GameEscrow-Production-Audit.md](./R7.69D-GameEscrow-Production-Audit.md)
- [Mainnet-Architecture.md](./Mainnet-Architecture.md)
- [Mainnet-Launch-Checklist.md](../operations/Mainnet-Launch-Checklist.md)
